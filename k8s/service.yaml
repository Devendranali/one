apiVersion: v1
kind: Service
metadata:
  name: one-app-service
spec:
  selector:
    app: one-app
  type: NodePort
  ports: 
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30001
    
